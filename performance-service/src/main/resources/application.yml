server:
  port: 8082
  servlet:
    context-path: /api

spring:
  application:
    name: performance-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
    metrics:
      enabled: true
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    com.supernova.integration: DEBUG
    org.springframework.web: DEBUG

info:
  app:
    name: ${spring.application.name}
    description: Performance Service for Developer Portal
    version: 1.0.0